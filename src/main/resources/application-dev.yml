spring:
  datasource:
    driver-class-name: org.h2.Driver
    url: 'jdbc:h2:file:./carrental-db'
    username: root
    password: example
  jpa:
    database-platform: org.hibernate.dialect.H2Dialect
    hibernate:
      ddl-auto: create-drop
  h2:
    console:
      enabled: true